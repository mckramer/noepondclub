- content_for :title, "Viewing dependent of #{@member.badge}"
%article
  %section
    %p
      %b Name:
      = @dependent.name
    %p
      %b Dob:
      = @dependent.dob
    %p
      = link_to "Edit", edit_dependent_path(@dependent)
      = link_to "Destroy", @dependent, :confirm => 'Are you sure?', :method => :delete
      = link_to "View All", dependents_path