- content_for :title, "Posts"
%article
  %section
    %p#notice= notice
    %h2= @post.title
    %p= @post.body
    %ol.links
      %li= link_to 'Edit', edit_post_path(@post) if member_signed_in?
      %li= link_to 'Back', posts_path
    .clear
    %hr
    = render 'comments'